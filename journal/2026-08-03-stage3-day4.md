# 2026-08-03 — stage3 day4: InMemoryRepository&lt;T&gt; (Repository 패턴)

## 오늘 푼 것

- `stages/stage3-generics/exercises/day4-repository.ts` — 테스트 6/6 통과 (`T extends { id: string }` 제약, save/upsert, findById/findAll, delete의 boolean 반환, 타입 레벨 거부)

## 배운 것

- (직접 채워주세요 — 왜 `T extends { id: string }` 제약이 필요한지: 제약이 없으면 findById가 성립하지 않는 이유)
- (TypeORM `Repository<Entity>`와 이 패턴의 관계)

## 막힌 점 / 에러

- (직접 채워주세요 — 없었으면 "없음")

## 다음에 할 것

- **Day 3 되돌아가기**: `notes/day3-comparable.md` → 자기 참조 제약 `Comparable<T>` (`npx jest day3-comparable --watch`) — 아직 미완(6개 실패)
- Day 5: 유틸리티 타입 결합 (`day5-utility-repository`)
