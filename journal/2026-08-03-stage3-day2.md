# 2026-08-03 — stage3 day2: 제네릭 제약 (T extends·keyof)

## 오늘 푼 것

- `stages/stage3-generics/exercises/day2-constraints.ts` — 테스트 7/7 통과 (LengthTracker의 `T extends {length}` 제약, PropertyPicker의 `K extends keyof T`, 타입 레벨 거부 2건)

## 배운 것

- (직접 채워주세요 — "제약 = 접근 권한"이라는 관점: extends가 없으면 왜 T의 프로퍼티를 못 건드리는지)
- (keyof T와 T[K]가 각각 무엇을 뜻하는지 내 말로)

## 막힌 점 / 에러

- (직접 채워주세요 — 없었으면 "없음")

## 다음에 할 것

- Day 3: `notes/day3-comparable.md` 읽기 → 제네릭 + 인터페이스 결합, 자기 참조 `Comparable<T>` (`npx jest day3-comparable --watch`)
